# Discord Bot Token (from Discord Developer Portal → Bot → Token)
DISCORD_BOT_TOKEN=your-bot-token-here

# Discord Application ID (from Discord Developer Portal → General Information)
DISCORD_CLIENT_ID=your-client-id-here

# (Optional) Guild ID for development — commands register instantly for this guild.
# Remove or leave empty for global command registration.
DISCORD_GUILD_ID=your-development-server-id

# (Optional) Maximum timer runtime in hours (1–24, default: 2).
# Timers auto-stop after this duration even if repeat count is not exhausted.
MAX_TIMER_HOURS=2

# (Optional) Interactions mode: "gateway" (default) or "http".
# Gateway uses WebSocket (discord.js Client). HTTP uses Discord's HTTP interactions endpoint.
INTERACTIONS_MODE=gateway

# (Optional) Discord Application Public Key (from Developer Portal → General Information).
# Required only for INTERACTIONS_MODE=http (used for Ed25519 signature verification).
# DISCORD_PUBLIC_KEY=your-public-key-hex

# (Optional) HTTP server port, used when INTERACTIONS_MODE=http. Default: 3000.
# PORT=3000

# (Optional) Storage backend: "memory" (default) or "redis".
# Memory requires no external services. Redis enables persistence across restarts.
STORAGE_BACKEND=memory

# (Required if STORAGE_BACKEND=redis) Upstash Redis credentials.
# On Fly.io: fly redis create, then fly secrets set UPSTASH_REDIS_REST_URL=... UPSTASH_REDIS_REST_TOKEN=...
# UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token-here

#https://discord.com/oauth2/authorize?client_id=DISCORD_CLIENT_ID&scope=bot%20applications.commands&permissions=18432
